export { Maybe
       } from 'ramda-fantasy';
export { Future
       } from 'fluture';
export { compose,
         concat,
         difference,
         groupBy,
         intersection,
         map,
         mapObjIndexed,
         mergeAll,
         once,
         path,
         pick,
         pipe,
         prop,
         reduce,
         sortBy,
         uniq,
         values,
         zip,
         zipObj,
         zipWith
       } from 'ramda';
export { Futureall } from './utils';
